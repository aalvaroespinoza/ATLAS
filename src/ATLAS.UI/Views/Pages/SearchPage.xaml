<Page
    x:Class="ATLAS.UI.Views.Pages.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:ATLAS.UI.ViewModels"
    mc:Ignorable="d">

    <Grid Padding="32,24" RowDefinitions="Auto,Auto,*">

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="4" Margin="0,0,0,16">
            <TextBlock Text="Buscar (Second Brain)" FontSize="26" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="Explorá tus notas capturadas y conocimiento indexado con búsqueda en vivo." FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <!-- Search Box -->
        <AutoSuggestBox
            Grid.Row="1"
            Margin="0,0,0,16"
            MaxWidth="600"
            HorizontalAlignment="Left"
            PlaceholderText="Buscar notas por contenido, título o tags..."
            QueryIcon="Find"
            Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay}" />

        <!-- Main Content Grid (Master-Detail) -->
        <Grid Grid.Row="2" ColumnDefinitions="360,*" ColumnSpacing="24">

            <!-- Left: Notes List -->
            <Border
                Grid.Column="0"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8">

                <Grid RowDefinitions="*">
                    <ListView
                        ItemsSource="{x:Bind ViewModel.Notes}"
                        SelectedItem="{x:Bind ViewModel.SelectedNote, Mode=TwoWay}"
                        SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:NoteItemViewModel">
                                <StackPanel Padding="4,6" Spacing="2">
                                    <TextBlock Text="{x:Bind DisplayTitle}" FontWeight="SemiBold" FontSize="13" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{x:Bind PreviewContent}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{x:Bind FormattedDate}" FontSize="10" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <TextBlock
                        Text="No se encontraron notas."
                        FontSize="13"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="{x:Bind ViewModel.HasNoResultsVisibility, Mode=OneWay}" />
                </Grid>
            </Border>

            <!-- Right: Note Detail View -->
            <Border
                Grid.Column="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24">

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="16">
                        <StackPanel Spacing="4">
                            <TextBlock
                                Text="{x:Bind ViewModel.SelectedNote.DisplayTitle, Mode=OneWay}"
                                FontSize="20"
                                FontWeight="Bold" />
                            <TextBlock
                                Text="{x:Bind ViewModel.SelectedNote.FormattedDate, Mode=OneWay}"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>

                        <MenuFlyoutSeparator />

                        <TextBlock
                            Text="{x:Bind ViewModel.SelectedNote.Content, Mode=OneWay}"
                            FontSize="14"
                            TextWrapping="Wrap"
                            IsTextSelectionEnabled="True"
                            LineHeight="22" />

                        <!-- AI Summarize Area -->
                        <Border
                            Margin="0,16,0,0"
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="16">
                            <StackPanel Spacing="10">
                                <Grid ColumnDefinitions="*,Auto">
                                    <TextBlock Text="Resumen Asistido (Gemini)" FontWeight="SemiBold" FontSize="12" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                                    <Button
                                        Grid.Column="1"
                                        Content="Resumir con IA"
                                        FontSize="11"
                                        Padding="10,4"
                                        Command="{x:Bind ViewModel.SummarizeSelectedNoteCommand}" />
                                </Grid>

                                <ProgressBar
                                    IsIndeterminate="True"
                                    Visibility="{x:Bind ViewModel.IsAiSummarizingVisibility, Mode=OneWay}" />

                                <TextBlock
                                    Text="{x:Bind ViewModel.AiSummary, Mode=OneWay}"
                                    FontSize="12"
                                    TextWrapping="Wrap"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Visibility="{x:Bind ViewModel.HasAiSummaryVisibility, Mode=OneWay}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
